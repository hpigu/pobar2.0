<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pobar.mapper.ProductMapper">

    <select id="selectProductIdsByIngredient" resultType="java.lang.Integer">
        SELECT DISTINCT p.id
        FROM product p
        INNER JOIN recipe r ON r.product_id = p.id
        INNER JOIN recipe_ingredient ri ON ri.recipe_id = r.id
        WHERE ri.ingredient_id = #{ingredientId}
          AND p.is_active = 1
    </select>

</mapper>
